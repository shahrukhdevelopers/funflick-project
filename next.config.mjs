/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['funflick-toys.s3.eu-north-1.amazonaws.com','funflick-toys.s3.amazonaws.com'],
    }
};

export default nextConfig;